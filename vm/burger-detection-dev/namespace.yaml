---
- hosts: localhost
  tasks:
  - name: create a user
    ansible.builtin.user:
        name: dev-burger-detection-dev
  - name: cope platform configs
    ansible.builtin.copy:
        src: ./platform-config.sh
        dest: /home/dev-burger-detection-dev/platform-config.sh
        owner: dev-burger-detection-dev
        group: dev-burger-detection-dev
        mode: u+rw,g-wx,o-rwx
